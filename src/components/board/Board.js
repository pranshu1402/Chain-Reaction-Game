import React from 'react';

const Board = () => {
    return (
        <div>
            {/* This will present the whole game board created using blocks
             along with leaderboard */}
            {/* Game  Status */}
            Board
            {/* Leaderboard */}
        </div>
    );
};

export default Board;